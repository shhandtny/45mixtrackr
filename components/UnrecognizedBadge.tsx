'use client';

export function UnrecognizedBadge() {
  return (
    <span className="inline-block text-[10px] font-semibold uppercase tracking-wider text-spotify-dim border border-spotify-dim rounded px-1.5 py-0.5">
      Unrecognized
    </span>
  );
}
